name: tests

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  pytest:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    defaults:
      run:
        shell: bash -el {0}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Conda environment
        uses: conda-incubator/setup-miniconda@v3
        with:
          activate-environment: OpenCompany
          environment-file: environment.yml
          auto-activate-base: false
          miniforge-version: latest

      - name: Run full test suite
        run: pytest -q

  repo-contracts:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        shell: bash -el {0}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Conda environment
        uses: conda-incubator/setup-miniconda@v3
        with:
          activate-environment: OpenCompany
          environment-file: environment.yml
          auto-activate-base: false
          miniforge-version: latest

      - name: Run repository contracts
        run: pytest -q tests/test_repo_contracts.py

  static-quality:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        shell: bash -el {0}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Conda environment
        uses: conda-incubator/setup-miniconda@v3
        with:
          activate-environment: OpenCompany
          environment-file: environment.yml
          auto-activate-base: false
          miniforge-version: latest

      - name: Run Ruff
        run: ruff check .

      - name: Run MyPy
        run: mypy
